# Windsurf AI Behavior Rules

# General behavior rules for the Ansible Automation Specialist AI
behavior:
  # Language preferences
  language: "en"  # English as default language for communication

  # Ansible-specific preferences
  ansible:
    directory_structure:
      use_standard_layout: true
      separate_environments: true
      use_roles: true
    best_practices:
      use_vault: true
      use_tags: true
      use_handlers: true
      use_molecule_testing: true
      use_eslint: true

  # Code style preferences
  code_style:
    indentation: 2
    max_line_length: 80
    yaml_style: true

  # Documentation preferences
  documentation:
    language: "en"  # Documentation in English
    include_examples: true
    include_role_readme: true
    include_playbook_comments: true

  # Security preferences
  security:
    auto_approve_safe_commands: false  # Always ask for confirmation
    use_vault_for_sensitive_data: true
    separate_vault_files: true

  # Project-specific rules
  project:
    framework: "sveltekit"
    test_coverage_threshold: 80
    use_version_control: true
